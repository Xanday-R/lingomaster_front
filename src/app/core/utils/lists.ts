import { Levels } from '@core/enums/levels.enum';
import { Formats } from '@core/enums/formats.enum';
import { Languages } from '@core/enums/languages.enum';
import { ModelsPractice } from '@core/enums/models-practice.enum';

export const LanguagesList:Languages[] = Object.values(Languages);

export const FormatsList:Formats[] = Object.values(Formats);

export const LevelsList:Levels[] = Object.values(Levels);

export const ModelPracticeList: ModelsPractice[] = Object.values(ModelsPractice);
